{% extends 'base.html.twig' %}

{% block title %}Hello UploadImageController!
{% endblock %}


{% block stylesheets %}
	{{ parent() }}
	<link rel="stylesheet" href="{{ asset('assets/css/custom-slider.css') }}">
{% endblock %}

{% block body %}

	{{form_start(formulaire)}}

	{# {{form_widget(formulaire)}} #}
	<div class="card mb-3" style="width: 25%; margin-top:5%; display:flex;margin-left:auto;margin-right:auto;">
		<div class="card-body">
			<h3 class="card-title" style="text-align: center">Ajouter une image </h3>
		</div>
		{# <div class="image-container">
		{% if imageFileName is not null %}
    <img src="{{ asset('DownloadedImage/' ~ imageFileName) }}" alt="UploadedImage" style="width:100%">
{% else %}
    <p>Aucune image sélectionnée</p>
{% endif %}


		</div> #}
	</div>
	<div class="form_group"style=" margin-top:5%;margin-bottom:2%; text-align:center;">
	
	    
		{{form_row(formulaire.UploadedImage,{"label":"Veuillez choisir une image","attr":{"placeholder":"", "class":"form-control"}})}}
	
        <small class="text-danger">Seul les formats JPEG et PNG sont supportés</small>    
	</div>

      
<button type="submit" class="btn btn-primary" style="display: block; margin: auto;">Ajouter</button>


	{{form_end(formulaire)}}
{% endblock %}
